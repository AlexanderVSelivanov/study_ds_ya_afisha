# Анализ лояльности пользователей Яндекс Афиши

Install requirements:

```bash
pip install -r requirements.txt
```

Initialize .env file:

```bash
echo -e "
DB_USER = 
DB_PWD = 
DB_HOST = 
DB_PORT = 
DB_NAME = 
" > .env
```
